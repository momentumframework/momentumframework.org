scripts:
  start: deno run -A --unstable -c tsconfig.json src/main.ts
  start:dev: deno run -A --watch --unstable -c tsconfig.json src/main.ts
  test: deno test --allow-net src/
  format: deno fmt src/